# M1-S1 · 脚手架 + Token + 静态桌面

**日期**: 2026-04-08
**阶段**: M1-S1（V1 第一个阶段）
**目标**: 用户能打开页面，看到 iPhone 外壳 + 状态栏 + 壁纸 + 桌面图标网格 + Dock，最快获得可见反馈。

---

## 交付清单

1. 项目脚手架（Vite + React 18 + TS strict + Tailwind v4 + Vitest + RTL + ESLint）
2. Design Token 全量浅色值（typography / spacing / radius / materials / motion / colors）
3. Tailwind config 从 Token 派生
4. Device 外壳 + StatusBar（实时时间）+ DynamicIsland（静态胶囊）+ HomeIndicator
5. Springboard 静态两页（69 图标 + Dock + 页面指示器）
6. Material 原子组件（Liquid Glass 唯一出口）
7. 全部子目录 CLAUDE.md 规范
8. 单测全绿 + build 无 warning

---

## 测试优先（先写测试再写实现）

| 测试文件 | 验证内容 |
|---|---|
| `src/platform/design-tokens/__tests__/tokens.test.ts` | Token 对象结构完整 |
| `src/platform/design-tokens/__tests__/concentric.test.ts` | `concentric(22, 4) === 18` |
| `src/shell/StatusBar/__tests__/useClock.test.ts` | mock Date → 验证 "08:04" 格式 |
| `src/shell/Springboard/__tests__/AppIcon.test.tsx` | 渲染 alt 文本 + class 含 rounded-icon |
| `src/shell/Device/__tests__/Device.test.tsx` | 渲染不报错 + 含 wallpaper 背景 |
| `src/system/Material/__tests__/Material.test.tsx` | variant thin/regular/thick/chrome 各渲染 backdrop-filter |

---

## 手工验收清单

- [ ] `pnpm dev` → 浏览器看到 iPhone 外壳
- [ ] 状态栏显示实时时间
- [ ] Dynamic Island 黑色胶囊居中
- [ ] 底部 Home Indicator 横条可见
- [ ] 69 个图标排列为网格（两页内容）
- [ ] Dock 底部 4 个图标 + 毛玻璃背景
- [ ] 图标按下有 scale 缩放
- [ ] 背景为壁纸 ios-26-stock-01
