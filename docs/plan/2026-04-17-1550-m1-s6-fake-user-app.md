# M1 · S6: Fake User App + E2E Verification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 S1-S5 串起来：硬编码一段使用 `@hiphone/ui` 的 TSX → 编译 → 沙箱 → wrap → 注册；在 DEV 模式下加一个桌面图标触发这个流程，再加集成测试锁住整条链路。

**Architecture:** `fakeUserApp.ts` 导出 TSX 字符串常量 + id 常量。`devIcon.ts` 的 `mountFakeUserAppIfDev()` 在 DEV 模式下执行完整管道并注册到 `appRegistry`。`apps.data.ts` 在 DEV 模式下追加一个桌面图标。`App.tsx` 启动时调用 `mountFakeUserAppIfDev()`。生产 build 时 `import.meta.env.DEV` 为 false，Vite tree-shake 剔除所有 DEV 代码。

**Tech Stack:** 前 5 个 stage 的所有成果 + Vite 环境变量 + `@testing-library/react`.

**Parent spec:** [2026-04-17-m1-architecture-decoupling-design.md](../superpowers/specs/2026-04-17-m1-architecture-decoupling-design.md) §S6

**Prerequisites:** S1-S5 全部完成

**Test command:** `pnpm test` (single-run vitest). Build 验证用 `pnpm build`.

---

## File Structure

- **Create** `src/platform/userApp/fakeUserApp.ts` — TSX 源码常量 + id 常量
- **Create** `src/platform/userApp/devIcon.ts` — `mountFakeUserAppIfDev()`
- **Create** `src/platform/userApp/__tests__/fakeUserApp.integration.test.tsx` — 端到端集成测试
- **Modify** `src/shell/Springboard/apps.data.ts` — DEV 模式追加假 app 图标
- **Modify** `src/App.tsx` — 启动时调用 mountFakeUserAppIfDev

---

## Task 1: Fake User App Source + Constants

**Files:**
- Create: `src/platform/userApp/fakeUserApp.ts`

- [ ] **Step 1: Create fake app constants**

Create `src/platform/userApp/fakeUserApp.ts`:

```typescript
/**
 * Dev-only: a hardcoded TSX string used to verify the end-to-end
 * user-app pipeline (compile → sandbox → wrap → register → render).
 *
 * Imports only what M1's minimal SDK provides:
 * - 'react' — React namespace
 * - '@hiphone/ui' — NavBar
 *
 * Removed in production builds via import.meta.env.DEV gating in
 * devIcon.ts and apps.data.ts.
 */
export const FAKE_USER_APP_ID = 'dev-fake-user-app';
export const FAKE_USER_APP_NAME = '[DEV] 假用户 app';

export const FAKE_USER_APP_SOURCE = `
import React from 'react';
import { NavBar } from '@hiphone/ui';

export default function FakeUserApp() {
  return (
    <div style={{ height: '100%', backgroundColor: 'var(--color-systemBackground)' }}>
      <NavBar title="假用户 app" />
      <div style={{ padding: 20, fontSize: 17, color: 'var(--color-label)' }}>
        Hello from sandbox!
      </div>
    </div>
  );
}
`;
```

- [ ] **Step 2: Typecheck**

Run: `pnpm typecheck`
Expected: no errors.

- [ ] **Step 3: Commit (constants only; pipeline comes next)**

```bash
git add src/platform/userApp/fakeUserApp.ts
git commit -m "$(cat <<'EOF'
feat(userApp): add hardcoded fake TSX source for M1 e2e verification

M1 S6 Task 1 — 一段使用 @hiphone/ui NavBar 的极简 TSX，用于验证
M1 管道通畅。不包含 M2+ SDK 的任何调用。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: Dev Icon Mount Function

**Files:**
- Create: `src/platform/userApp/devIcon.ts`
- Create: `src/platform/userApp/__tests__/fakeUserApp.integration.test.tsx`

- [ ] **Step 1: Write failing integration test**

Create `src/platform/userApp/__tests__/fakeUserApp.integration.test.tsx`:

```typescript
import { render, cleanup } from '@testing-library/react';
import { afterEach, beforeEach, describe, expect, it } from 'vitest';
import { appRegistry } from '@/platform/appRegistry';
import { AppScene } from '@/apps/AppScene';
import { mountFakeUserApp } from '../devIcon';
import { FAKE_USER_APP_ID } from '../fakeUserApp';

describe('M1 e2e — fake user app pipeline', () => {
  beforeEach(() => {
    appRegistry.list().forEach((e) => appRegistry.unregister(e.id));
  });

  afterEach(() => {
    cleanup();
    appRegistry.list().forEach((e) => appRegistry.unregister(e.id));
  });

  it('compiles, sandboxes, wraps, and registers the fake app', async () => {
    await mountFakeUserApp();

    const entry = appRegistry.get(FAKE_USER_APP_ID);
    expect(entry).toBeDefined();
    expect(entry?.type).toBe('user');
    expect(typeof entry?.component).toBe('function');
  });

  it('AppScene renders the fake app after mounting', async () => {
    await mountFakeUserApp();

    const { container } = render(<AppScene appId={FAKE_USER_APP_ID} />);

    expect(container.textContent).toContain('假用户 app');
    expect(container.textContent).toContain('Hello from sandbox!');
  });

  it('calling mountFakeUserApp twice is idempotent', async () => {
    await mountFakeUserApp();
    const firstComponent = appRegistry.get(FAKE_USER_APP_ID)?.component;

    await mountFakeUserApp();
    const secondComponent = appRegistry.get(FAKE_USER_APP_ID)?.component;

    // Re-registration allowed; we just care both calls succeeded.
    expect(firstComponent).toBeDefined();
    expect(secondComponent).toBeDefined();
  });
});
```

- [ ] **Step 2: Run — verify fails (module missing)**

Run: `pnpm test src/platform/userApp/__tests__/fakeUserApp.integration.test.tsx`
Expected: FAIL — cannot import `../devIcon`.

- [ ] **Step 3: Create devIcon.ts**

Create `src/platform/userApp/devIcon.ts`:

```typescript
import { appRegistry } from '@/platform/appRegistry';
import { compileTsx } from './compiler';
import { executeSandboxed } from './sandbox';
import { resolveModule } from './sdk';
import { wrapUserComponent } from './sdk/wrap';
import { FAKE_USER_APP_ID, FAKE_USER_APP_SOURCE } from './fakeUserApp';

/**
 * Run the full user-app pipeline on the hardcoded fake source, and
 * register the result in appRegistry. Exported separately from the
 * DEV-gated entry point so tests (which may run under Vite's test
 * mode where `import.meta.env.DEV` can be true) can invoke it
 * deterministically.
 *
 * Usage:
 * - Tests: call `mountFakeUserApp()` directly
 * - App.tsx: call `mountFakeUserAppIfDev()` (DEV-only)
 */
export async function mountFakeUserApp(): Promise<void> {
  const compiled = await compileTsx(FAKE_USER_APP_SOURCE);
  const RawComponent = executeSandboxed(compiled, resolveModule);
  const WrappedComponent = wrapUserComponent(RawComponent);

  appRegistry.register({
    id: FAKE_USER_APP_ID,
    type: 'user',
    component: WrappedComponent,
    perspectiveAware: false,
    globalData: true, // viewing another's phone still shows the dev demo
  });
}

/**
 * DEV-gated entry point called from App.tsx at startup.
 * In production builds, `import.meta.env.DEV` is false and Vite's
 * dead-code elimination removes this function body entirely.
 */
export async function mountFakeUserAppIfDev(): Promise<void> {
  if (!import.meta.env.DEV) return;
  try {
    await mountFakeUserApp();
  } catch (err) {
    console.error('[DEV] Failed to mount fake user app:', err);
  }
}
```

- [ ] **Step 4: Run integration test — verify passes**

Run: `pnpm test src/platform/userApp/__tests__/fakeUserApp.integration.test.tsx`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add src/platform/userApp/devIcon.ts src/platform/userApp/__tests__/fakeUserApp.integration.test.tsx
git commit -m "$(cat <<'EOF'
feat(userApp): wire fake user app pipeline end-to-end

M1 S6 Task 2 — mountFakeUserApp 把 S4 compiler + S4 sandbox + S5
resolver + S5 wrapper + S1 registry 串起来。mountFakeUserAppIfDev
是 DEV 守卫入口，生产 build 被 tree-shake 剔除。集成测试覆盖完整
pipeline + AppScene 渲染链路。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Dev Icon on Springboard

**Files:**
- Modify: `src/shell/Springboard/apps.data.ts`

- [ ] **Step 1: Pick a placeholder icon path**

Run: `ls public/resource/icons/` (or wherever icons live — check by reading current apps.data.ts which uses paths like `/resource/icons/ios-system/settings.jpg`).

We'll use an existing ios-system icon as placeholder for the dev icon (no new SVG needed). Pick one that's visually distinct — e.g. `tips.jpg`.

- [ ] **Step 2: Modify apps.data.ts to add DEV icon**

In `src/shell/Springboard/apps.data.ts`, find the `cnApps` array definition and add a DEV-only push right before the `export const apps` line.

Add this block after the `cnApps` const definition (around line 74, right after the closing `]` of `cnApps`) and before `/** Dock apps — fixed 4 slots */`:

```typescript
// [DEV] Fake user app icon — for M1 pipeline verification. In production
// builds, import.meta.env.DEV is false and Vite's dead-code elimination
// drops this entry entirely.
if (import.meta.env.DEV) {
  cnApps.push({
    id: 'dev-fake-user-app',
    name: '[DEV] 假用户 app',
    icon: `${SYSTEM_ICON_BASE}/tips.jpg`,
    page: 1,
  });
}
```

(The id string matches `FAKE_USER_APP_ID` in `fakeUserApp.ts`. Kept as a literal here to avoid circular imports.)

- [ ] **Step 3: Typecheck**

Run: `pnpm typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add src/shell/Springboard/apps.data.ts
git commit -m "$(cat <<'EOF'
feat(springboard): add DEV-only [DEV] fake user app icon

M1 S6 Task 3 — Dev 模式下桌面第 2 页多一个 [DEV] 假用户 app 图标。
点击后走 Registry → AppScene 完整路径（Registry 里已有 user-app
entry 由 mountFakeUserAppIfDev 注册）。生产 build 被剔除。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Wire mountFakeUserAppIfDev into App.tsx

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Modify App.tsx**

Replace `src/App.tsx`:

```typescript
import { useEffect } from 'react';
import { Device } from './shell/Device';
import { MusicPlaybackHost } from './apps/Music/MusicPlaybackHost';
import { startHeartbeatScheduler } from './platform/ai/heartbeatAgent';
import { registerBuiltins } from './apps/registerBuiltins';
import { mountFakeUserAppIfDev } from './platform/userApp/devIcon';

// Register all builtin apps into the Registry at module load.
// Safe to run at module scope: registerBuiltins is idempotent.
registerBuiltins();

export function App() {
  useEffect(() => {
    startHeartbeatScheduler();
    // DEV-only: mount the hardcoded fake user app so the [DEV] icon
    // on the springboard works. Production builds skip this entirely
    // via import.meta.env.DEV tree-shaking.
    mountFakeUserAppIfDev();
  }, []);

  return (
    <>
      {/* Always-on audio engine: drives playback from the music store so the
          widget's play/pause/skip buttons work even when Music.app is not
          in the foreground. Renders nothing. */}
      <MusicPlaybackHost />
      <Device />
    </>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `pnpm typecheck`
Expected: no errors.

- [ ] **Step 3: Run full test suite**

Run: `pnpm test`
Expected: all tests pass (existing + S1-S6 new tests).

- [ ] **Step 4: Smoke test in dev mode**

Run: `pnpm dev` — open `http://localhost:5173/` and:
- [ ] Swipe to page 2 of Springboard — see `[DEV] 假用户 app` icon
- [ ] Tap the icon — app opens, shows NavBar with title "假用户 app" and "Hello from sandbox!" body text
- [ ] Return to home — icon disappears (normal behavior, app closes)
- [ ] Tap again — same app reopens correctly
- [ ] Verify all existing built-in apps (Settings, XingYu, Weather, Notes) still open correctly — no regressions

Stop the dev server (Ctrl+C).

- [ ] **Step 5: Commit**

```bash
git add src/App.tsx
git commit -m "$(cat <<'EOF'
feat(app): mount fake user app in DEV mode on startup

M1 S6 Task 4 — App.tsx 启动时触发 mountFakeUserAppIfDev，让 [DEV]
假用户 app 图标可点击。生产 build 中这个 effect 的 body 因
import.meta.env.DEV === false 被 tree-shake 剔除。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Production Build Verification

**Files:** none (verification-only).

- [ ] **Step 1: Run production build**

Run: `pnpm build`
Expected: build succeeds, outputs to `dist/`.

- [ ] **Step 2: Verify dev artifacts are stripped**

Run:
```bash
grep -r "Hello from sandbox" dist/ || echo "OK: not found"
grep -r "dev-fake-user-app" dist/ || echo "OK: not found"
grep -r "FAKE_USER_APP_SOURCE" dist/ || echo "OK: not found"
```

Expected: all three print `OK: not found`.

If any are found, `import.meta.env.DEV` tree-shaking is not working — investigate and fix before proceeding.

- [ ] **Step 3: Verify Sucrase is bundled lazily (only when triggered)**

Run:
```bash
ls dist/assets/ | grep -i sucrase
```

Expected: at least one Sucrase-containing chunk exists (it's still a dependency). It may be in its own chunk or merged with the user-app code path, depending on Vite's chunk splitting heuristics. Presence is fine; what matters is `dist/index.html` main bundle doesn't eagerly import it — but verifying this requires bundle analysis and is out of scope for M1.

- [ ] **Step 4: Deploy + verify on production URL**

Follow `docs/skills/hiphone-cloudflare-pages-deploy/SKILL.md`:

```bash
npx -y wrangler pages deploy dist --project-name mini-iphone --commit-dirty=true
```

Open `https://mini-iphone.pages.dev/` and verify:
- [ ] Page 2 of Springboard does **NOT** show `[DEV] 假用户 app`
- [ ] All normal builtin apps still work

- [ ] **Step 5: (No commit — verification only)**

This task has no file changes; the production build output should not be committed.

---

## Acceptance Criteria — M1 Complete

- [ ] 3 integration tests pass (`fakeUserApp.integration.test.tsx`)
- [ ] `pnpm test` all green (all existing + all M1 new tests)
- [ ] `pnpm typecheck` passes
- [ ] **Dev mode (`pnpm dev`)**: `[DEV] 假用户 app` icon on page 2, tap shows "Hello from sandbox!"
- [ ] **Production build**: `grep` for dev strings in `dist/` returns empty
- [ ] **Production deployment**: No `[DEV]` icon visible on `https://mini-iphone.pages.dev/`
- [ ] All builtin apps still open and work (manual regression)
- [ ] 4 commits on branch:
  - Task 1 fake source
  - Task 2 devIcon + integration test
  - Task 3 DEV springboard icon
  - Task 4 wire into App.tsx
  - (Task 5 is verification-only, no commit)

## M1 Full Milestone Verification

When all S1-S6 acceptance criteria met, M1 is complete. Check parent spec §里程碑验收 list:

1. [ ] All existing builtin apps 行为 100% 不变
2. [ ] App Registry 注册所有内置 app，AppScene 改为查表分发
3. [ ] appRuntimeStore 在 4 类状态转换时正确发射 nonce
4. [ ] Compiler 能把硬编码 TSX 编译成可执行 JS
5. [ ] Sandbox 能执行编译后 JS 并返回 React 组件，遮蔽常见全局
6. [ ] `@hiphone/ui` 的 NavBar 能被沙箱内用户代码 import
7. [ ] Dev 模式下 `[DEV] 假用户 app` 图标点击后显示 "Hello from sandbox!"
8. [ ] 生产构建中无 Dev 图标/假 app 代码
9. [ ] 所有单元测试 + 集成测试绿灯
10. [ ] 部署到 Cloudflare Pages 验证通过

M1 完成后进入 M2（完整安装流程 + 基础 SDK）。
