# M2 · S3: @hiphone/storage (Flat Key-Value) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 user app 能通过 `@hiphone/storage` 做 IDB 持久化。S3 用扁平 key（`{appId}:{userKey}` / `{appId}:__global__:{userKey}`），S4 再加 owner 维度。

**Architecture:** `appStorage.ts` 是 `app-kv` object store 的底层读写（get/set/remove/listByAppId/deleteAllByAppId）。`sdk/storage.ts` 在 appStorage 之上包 SDK surface：根据 SDK 调用上下文里的 `appId` 自动构造 full key。`createUserAppRuntime` 把 appId 注入到 SDK 的执行上下文里。

**Tech Stack:** TypeScript, Vitest, IndexedDB (via existing `idbStorage.ts`), pnpm.

**Parent spec:** [2026-04-17-m2-user-app-install-design.md](../superpowers/specs/2026-04-17-m2-user-app-install-design.md) §S3 + §2.3

**Test command:** `pnpm test`

---

## File Structure

- **Create** `src/platform/userApp/appStorage.ts` — IDB 原语 (get/set/remove/listByAppId/deleteAllByAppId)
- **Create** `src/platform/userApp/__tests__/appStorage.test.ts`
- **Create** `src/platform/userApp/sdk/storage.ts` — `@hiphone/storage` SDK (get/set/remove/list/globalGet/globalSet)
- **Create** `src/platform/userApp/sdk/__tests__/storage.test.ts`
- **Modify** `src/platform/userApp/sdk/index.ts` — 增加 moduleMap 入口
- **Modify** `src/platform/userApp/moduleResolver.ts` — 把 appId 注入 SDK 上下文
- **Create** `src/platform/userApp/sdk/context.ts` — 存当前运行中的 appId（`AsyncLocalStorage` 不可用；改用显式 context + setCurrentAppId getter）

---

## Task 1: appStorage.ts — IDB primitives

**Files:**
- Create: `src/platform/userApp/appStorage.ts`
- Create: `src/platform/userApp/__tests__/appStorage.test.ts`

- [ ] **Step 1: Write tests**

Create `src/platform/userApp/__tests__/appStorage.test.ts`:

```typescript
import { beforeEach, describe, expect, it } from 'vitest';
import 'fake-indexeddb/auto';
import {
  appStorageGet,
  appStorageSet,
  appStorageRemove,
  appStorageListByAppId,
  appStorageDeleteAllByAppId,
} from '../appStorage';
import { getDB, APP_KV_STORE } from '@/platform/storage/idbStorage';

async function clearKv(): Promise<void> {
  const db = await getDB();
  await new Promise<void>((resolve) => {
    const tx = db.transaction(APP_KV_STORE, 'readwrite');
    tx.objectStore(APP_KV_STORE).clear();
    tx.oncomplete = () => resolve();
  });
}

describe('appStorage', () => {
  beforeEach(() => clearKv());

  it('set then get returns the value', async () => {
    await appStorageSet('todo', 'todo:items', {
      appId: 'todo',
      scope: 'app',
      ownerId: '',
      userKey: 'items',
      value: [1, 2, 3],
    });
    const got = await appStorageGet('todo', 'todo:items');
    expect(got).toEqual({
      appId: 'todo',
      scope: 'app',
      ownerId: '',
      userKey: 'items',
      value: [1, 2, 3],
    });
  });

  it('get returns undefined for missing key', async () => {
    expect(await appStorageGet('todo', 'todo:missing')).toBeUndefined();
  });

  it('remove deletes the value', async () => {
    await appStorageSet('todo', 'todo:x', {
      appId: 'todo', scope: 'app', ownerId: '', userKey: 'x', value: 1,
    });
    await appStorageRemove('todo', 'todo:x');
    expect(await appStorageGet('todo', 'todo:x')).toBeUndefined();
  });

  it('listByAppId returns all full keys for an appId', async () => {
    await appStorageSet('a', 'a:k1', { appId: 'a', scope: 'app', ownerId: '', userKey: 'k1', value: 1 });
    await appStorageSet('a', 'a:k2', { appId: 'a', scope: 'app', ownerId: '', userKey: 'k2', value: 2 });
    await appStorageSet('b', 'b:k1', { appId: 'b', scope: 'app', ownerId: '', userKey: 'k1', value: 3 });

    const keys = await appStorageListByAppId('a');
    expect(keys.sort()).toEqual(['a:k1', 'a:k2']);
  });

  it('deleteAllByAppId clears all rows for an appId only', async () => {
    await appStorageSet('a', 'a:k1', { appId: 'a', scope: 'app', ownerId: '', userKey: 'k1', value: 1 });
    await appStorageSet('b', 'b:k1', { appId: 'b', scope: 'app', ownerId: '', userKey: 'k1', value: 2 });

    await appStorageDeleteAllByAppId('a');

    expect(await appStorageGet('a', 'a:k1')).toBeUndefined();
    expect(await appStorageGet('b', 'b:k1')).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test — fails**

Run: `pnpm test src/platform/userApp/__tests__/appStorage.test.ts`
Expected: FAIL — module missing

- [ ] **Step 3: Implement `appStorage.ts`**

Create `src/platform/userApp/appStorage.ts`:

```typescript
import {
  getDB,
  APP_KV_STORE,
  APP_KV_BY_APP_INDEX,
} from '@/platform/storage/idbStorage';

/**
 * Shape of every row in the `app-kv` object store. The outer wrapper
 * structure lets us index by `appId` so uninstall can scan all rows
 * for a given app in one cursor pass.
 */
export interface AppKvRecord {
  appId: string;
  scope: 'app' | 'global' | 'owner';
  /** Owner id when scope === 'owner'; empty string otherwise. */
  ownerId: string;
  /** The key the user code passed to set() / globalSet(). */
  userKey: string;
  value: unknown;
}

export async function appStorageGet(
  appId: string,
  fullKey: string,
): Promise<AppKvRecord | undefined> {
  const db = await getDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(APP_KV_STORE, 'readonly');
    const req = tx.objectStore(APP_KV_STORE).get(fullKey);
    req.onsuccess = () => {
      const record = req.result as AppKvRecord | undefined;
      if (record && record.appId !== appId) {
        // Defensive: key collision across apps is prevented by design,
        // but if it ever happens, treat as miss.
        resolve(undefined);
      } else {
        resolve(record);
      }
    };
    req.onerror = () => reject(req.error);
  });
}

export async function appStorageSet(
  appId: string,
  fullKey: string,
  record: AppKvRecord,
): Promise<void> {
  if (record.appId !== appId) {
    throw new Error(
      `appStorageSet: record.appId "${record.appId}" mismatches arg appId "${appId}"`,
    );
  }
  const db = await getDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(APP_KV_STORE, 'readwrite');
    tx.objectStore(APP_KV_STORE).put(record, fullKey);
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}

export async function appStorageRemove(
  appId: string,
  fullKey: string,
): Promise<void> {
  const db = await getDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(APP_KV_STORE, 'readwrite');
    tx.objectStore(APP_KV_STORE).delete(fullKey);
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
    void appId; // reserved for future audit logging
  });
}

export async function appStorageListByAppId(appId: string): Promise<string[]> {
  const db = await getDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(APP_KV_STORE, 'readonly');
    const store = tx.objectStore(APP_KV_STORE);
    const index = store.index(APP_KV_BY_APP_INDEX);
    const cursorReq = index.openKeyCursor(IDBKeyRange.only(appId));
    const keys: string[] = [];
    cursorReq.onsuccess = () => {
      const cursor = cursorReq.result;
      if (cursor) {
        keys.push(String(cursor.primaryKey));
        cursor.continue();
      }
    };
    tx.oncomplete = () => resolve(keys);
    tx.onerror = () => reject(tx.error);
  });
}

export async function appStorageDeleteAllByAppId(appId: string): Promise<void> {
  const db = await getDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(APP_KV_STORE, 'readwrite');
    const store = tx.objectStore(APP_KV_STORE);
    const index = store.index(APP_KV_BY_APP_INDEX);
    const cursorReq = index.openCursor(IDBKeyRange.only(appId));
    cursorReq.onsuccess = () => {
      const cursor = cursorReq.result;
      if (cursor) {
        cursor.delete();
        cursor.continue();
      }
    };
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
  });
}
```

- [ ] **Step 4: Run tests — pass**

Run: `pnpm test src/platform/userApp/__tests__/appStorage.test.ts`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add src/platform/userApp/appStorage.ts src/platform/userApp/__tests__/appStorage.test.ts
git commit -m "$(cat <<'EOF'
feat(userApp): appStorage — IDB primitives for app-kv store

M2 S3 — get/set/remove/listByAppId/deleteAllByAppId 底层读写。
记录结构 AppKvRecord { appId, scope, ownerId, userKey, value } —
scope/ownerId 为后续 S4 perspective 铺路，S3 先扁平用 scope='app'
+ ownerId='' 塞住。deleteAllByAppId 走 by-app-id 索引游标，O(n)
扫一次就够，不用先 listByAppId 再逐条 delete。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: SDK context — inject appId per-runtime

**Files:**
- Create: `src/platform/userApp/sdk/context.ts`
- Modify: `src/platform/userApp/moduleResolver.ts` — accept appId parameter, push/pop context around execution
- Modify: `src/platform/userApp/installer.ts` — pass appId to `createUserAppRuntime`

- [ ] **Step 1: Write test**

Create `src/platform/userApp/sdk/__tests__/context.test.ts`:

```typescript
import { describe, expect, it } from 'vitest';
import {
  getCurrentAppId,
  withUserAppContext,
  NoUserAppContextError,
} from '../context';

describe('user app SDK context', () => {
  it('throws outside a runtime context', () => {
    expect(() => getCurrentAppId()).toThrow(NoUserAppContextError);
  });

  it('provides appId inside withUserAppContext callback', () => {
    const result = withUserAppContext('my-todo', () => {
      return getCurrentAppId();
    });
    expect(result).toBe('my-todo');
  });

  it('restores previous value on nested contexts', () => {
    withUserAppContext('outer', () => {
      expect(getCurrentAppId()).toBe('outer');
      withUserAppContext('inner', () => {
        expect(getCurrentAppId()).toBe('inner');
      });
      expect(getCurrentAppId()).toBe('outer');
    });
  });

  it('restores absence after context exit', () => {
    withUserAppContext('x', () => getCurrentAppId());
    expect(() => getCurrentAppId()).toThrow(NoUserAppContextError);
  });
});
```

- [ ] **Step 2: Run test — fails**

Run: `pnpm test src/platform/userApp/sdk/__tests__/context.test.ts`
Expected: FAIL — module missing

- [ ] **Step 3: Implement `context.ts`**

Create `src/platform/userApp/sdk/context.ts`:

```typescript
/**
 * Synchronous runtime context for user app SDK calls.
 *
 * When a user app is being executed (inside `createUserAppRuntime`),
 * the runtime pushes the appId onto a stack. SDK functions like
 * `@hiphone/storage.set()` read `getCurrentAppId()` to know which
 * app they're serving.
 *
 * Sync-only by design: user app code that Sucrase emits is synchronous
 * (CommonJS require calls). Async SDK methods (await storage.get) may
 * resolve after the runtime context has exited — so SDK methods
 * capture the appId at the time of the call (which is inside the
 * runtime's synchronous execution) before awaiting anything.
 */

const stack: string[] = [];

export class NoUserAppContextError extends Error {
  constructor() {
    super(
      'No user app runtime context active. SDK calls like @hiphone/storage ' +
        'must be invoked from within a user app render/lifecycle path.',
    );
    this.name = 'NoUserAppContextError';
  }
}

export function getCurrentAppId(): string {
  const top = stack[stack.length - 1];
  if (top === undefined) throw new NoUserAppContextError();
  return top;
}

export function withUserAppContext<T>(appId: string, fn: () => T): T {
  stack.push(appId);
  try {
    return fn();
  } finally {
    stack.pop();
  }
}

/** Test-only: inspect stack depth. */
export function _debugStackDepth(): number {
  return stack.length;
}
```

- [ ] **Step 4: Run tests — pass**

Run: `pnpm test src/platform/userApp/sdk/__tests__/context.test.ts`
Expected: 4 passed

- [ ] **Step 5: Thread appId through moduleResolver**

Modify `src/platform/userApp/moduleResolver.ts`: change `createUserAppRuntime` signature to accept `appId`, wrap execution with `withUserAppContext`:

Locate the function definition and change it to:

```typescript
import { withUserAppContext } from './sdk/context';

export function createUserAppRuntime(
  compiledMap: Record<string, string>,
  entryPath: string,
  sdkResolve: ModuleResolver,
  appId: string,
): ComponentType {
  if (!Object.prototype.hasOwnProperty.call(compiledMap, entryPath)) {
    throw new Error(`createUserAppRuntime: entry "${entryPath}" not in compiledMap`);
  }

  const moduleCache = new Map<string, { exports: any }>();

  function requireFrom(fromPath: string): (specifier: string) => unknown {
    return (specifier: string) => {
      if (!specifier.startsWith('.')) {
        return sdkResolve(specifier);
      }
      const resolved = resolveRelativePath(fromPath, specifier, compiledMap);
      const cached = moduleCache.get(resolved);
      if (cached) return cached.exports;
      const module = { exports: {} as any };
      moduleCache.set(resolved, module);
      withUserAppContext(appId, () =>
        executeInSandbox(compiledMap[resolved]!, requireFrom(resolved), module),
      );
      return module.exports;
    };
  }

  const entryModule = { exports: {} as any };
  moduleCache.set(entryPath, entryModule);
  withUserAppContext(appId, () =>
    executeInSandbox(compiledMap[entryPath]!, requireFrom(entryPath), entryModule),
  );

  const Component = entryModule.exports.default;
  if (typeof Component !== 'function') {
    throw new Error(`Entry "${entryPath}" did not export a default React component`);
  }

  // Wrap returned Component so render-time SDK calls (inside hooks) also
  // see the context.
  return function UserAppRoot(props: any) {
    return withUserAppContext(appId, () => (Component as any)(props));
  };
}
```

- [ ] **Step 6: Update installer to pass appId**

In `src/platform/userApp/installer.ts`, update the single call site `buildUserAppComponent`:

```typescript
function buildUserAppComponent(
  manifest: UserAppManifest,
  compiledMap: Record<string, string>,
): ComponentType {
  const Raw = createUserAppRuntime(compiledMap, manifest.entry, resolveModule, manifest.id);
  return wrapUserComponent(Raw);
}
```

- [ ] **Step 7: Update moduleResolver tests (new signature)**

Modify `src/platform/userApp/__tests__/moduleResolver.test.ts`: every `createUserAppRuntime(compiledMap, entry, sdkResolveMock)` call now takes a fourth arg `'test-app'`. Grep-and-replace inside the file.

- [ ] **Step 8: Run tests — verify**

Run: `pnpm test`
Expected: all green

- [ ] **Step 9: Commit**

```bash
git add src/platform/userApp/sdk/context.ts src/platform/userApp/sdk/__tests__/context.test.ts src/platform/userApp/moduleResolver.ts src/platform/userApp/__tests__/moduleResolver.test.ts src/platform/userApp/installer.ts
git commit -m "$(cat <<'EOF'
feat(userApp): thread appId through runtime via sync sdk context

M2 S3 prep — withUserAppContext(appId, fn) pushes appId onto a
stack around user code execution; SDK modules read via
getCurrentAppId(). Called both at module-init time and at render
time (via a wrapping root component) so hooks / SDK calls inside
the React tree also see the context.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: @hiphone/storage SDK

**Files:**
- Create: `src/platform/userApp/sdk/storage.ts`
- Create: `src/platform/userApp/sdk/__tests__/storage.test.ts`
- Modify: `src/platform/userApp/sdk/index.ts` — 加 moduleMap 条目

- [ ] **Step 1: Write tests**

Create `src/platform/userApp/sdk/__tests__/storage.test.ts`:

```typescript
import { beforeEach, describe, expect, it } from 'vitest';
import 'fake-indexeddb/auto';
import * as storage from '../storage';
import { withUserAppContext } from '../context';
import { getDB, APP_KV_STORE } from '@/platform/storage/idbStorage';

async function clearKv(): Promise<void> {
  const db = await getDB();
  await new Promise<void>((resolve) => {
    const tx = db.transaction(APP_KV_STORE, 'readwrite');
    tx.objectStore(APP_KV_STORE).clear();
    tx.oncomplete = () => resolve();
  });
}

describe('@hiphone/storage (flat)', () => {
  beforeEach(() => clearKv());

  it('set + get round-trip inside app context', async () => {
    await withUserAppContext('todo', async () => {
      await storage.set('items', [1, 2, 3]);
      expect(await storage.get('items')).toEqual([1, 2, 3]);
    });
  });

  it('different apps have isolated keys', async () => {
    await withUserAppContext('a', async () => {
      await storage.set('k', 'A-value');
    });
    await withUserAppContext('b', async () => {
      await storage.set('k', 'B-value');
      expect(await storage.get('k')).toBe('B-value');
    });
    await withUserAppContext('a', async () => {
      expect(await storage.get('k')).toBe('A-value');
    });
  });

  it('globalSet + globalGet use __global__ scope', async () => {
    await withUserAppContext('todo', async () => {
      await storage.globalSet('theme', 'dark');
      expect(await storage.globalGet('theme')).toBe('dark');
    });
  });

  it('set and globalSet do not clash for the same user key', async () => {
    await withUserAppContext('todo', async () => {
      await storage.set('theme', 'per-user');
      await storage.globalSet('theme', 'global');
      expect(await storage.get('theme')).toBe('per-user');
      expect(await storage.globalGet('theme')).toBe('global');
    });
  });

  it('remove removes only the specified key', async () => {
    await withUserAppContext('todo', async () => {
      await storage.set('a', 1);
      await storage.set('b', 2);
      await storage.remove('a');
      expect(await storage.get('a')).toBeUndefined();
      expect(await storage.get('b')).toBe(2);
    });
  });

  it('list returns user keys (stripping prefix)', async () => {
    await withUserAppContext('todo', async () => {
      await storage.set('items', [1]);
      await storage.set('draft', 'x');
      await storage.globalSet('theme', 'dark');
      const keys = await storage.list();
      expect(keys.sort()).toEqual(['draft', 'items']); // globalSet key excluded from list()
    });
  });
});
```

- [ ] **Step 2: Run test — fails**

Run: `pnpm test src/platform/userApp/sdk/__tests__/storage.test.ts`
Expected: FAIL — module missing

- [ ] **Step 3: Implement `storage.ts`**

Create `src/platform/userApp/sdk/storage.ts`:

```typescript
import {
  appStorageGet,
  appStorageSet,
  appStorageRemove,
  appStorageListByAppId,
  type AppKvRecord,
} from '../appStorage';
import { getCurrentAppId } from './context';

/**
 * @hiphone/storage — user-app-facing key-value storage.
 *
 * M2 S3: flat namespace per app (`{appId}:{key}` for per-app,
 * `{appId}:__global__:{key}` for cross-context global). S4 will
 * add owner dimension between app and user key.
 */

const APP_PREFIX = (appId: string) => `${appId}:`;
const GLOBAL_PREFIX = (appId: string) => `${appId}:__global__:`;

export async function get(key: string): Promise<unknown> {
  const appId = getCurrentAppId();
  const fullKey = APP_PREFIX(appId) + key;
  const record = await appStorageGet(appId, fullKey);
  return record?.value;
}

export async function set(key: string, value: unknown): Promise<void> {
  const appId = getCurrentAppId();
  const fullKey = APP_PREFIX(appId) + key;
  const record: AppKvRecord = {
    appId, scope: 'app', ownerId: '', userKey: key, value,
  };
  await appStorageSet(appId, fullKey, record);
}

export async function remove(key: string): Promise<void> {
  const appId = getCurrentAppId();
  const fullKey = APP_PREFIX(appId) + key;
  await appStorageRemove(appId, fullKey);
}

export async function list(): Promise<string[]> {
  const appId = getCurrentAppId();
  const fullKeys = await appStorageListByAppId(appId);
  const appPrefix = APP_PREFIX(appId);
  const globalPrefix = GLOBAL_PREFIX(appId);
  const out: string[] = [];
  for (const k of fullKeys) {
    if (k.startsWith(globalPrefix)) continue; // global keys excluded from per-app list()
    if (k.startsWith(appPrefix)) out.push(k.slice(appPrefix.length));
  }
  return out;
}

export async function globalGet(key: string): Promise<unknown> {
  const appId = getCurrentAppId();
  const fullKey = GLOBAL_PREFIX(appId) + key;
  const record = await appStorageGet(appId, fullKey);
  return record?.value;
}

export async function globalSet(key: string, value: unknown): Promise<void> {
  const appId = getCurrentAppId();
  const fullKey = GLOBAL_PREFIX(appId) + key;
  const record: AppKvRecord = {
    appId, scope: 'global', ownerId: '', userKey: key, value,
  };
  await appStorageSet(appId, fullKey, record);
}
```

- [ ] **Step 4: Add to SDK moduleMap**

Modify `src/platform/userApp/sdk/index.ts`:

```typescript
import React from 'react';
import * as hiphoneUi from './ui';
import * as hiphoneStorage from './storage';

const moduleMap: Record<string, unknown> = {
  react: React,
  '@hiphone/ui': hiphoneUi,
  '@hiphone/storage': hiphoneStorage,
};

// ... keep the rest unchanged
```

- [ ] **Step 5: Run tests — pass**

Run: `pnpm test src/platform/userApp/sdk/__tests__/storage.test.ts`
Expected: 6 passed

- [ ] **Step 6: Run full test suite**

Run: `pnpm test`
Expected: all green

- [ ] **Step 7: Integration test — install todo app that uses storage**

Append to `src/platform/userApp/__tests__/installer.test.ts`:

```typescript
describe('installer — end-to-end with storage', () => {
  beforeEach(async () => {
    useInstalledUserAppsStore.setState({ apps: [] });
    appRegistry.list().forEach((e) => appRegistry.unregister(e.id));
    await resetIdb();
  });

  it('user app can write and read storage through @hiphone/storage', async () => {
    const zip = await makeZip({
      'manifest.json': JSON.stringify({
        id: 'storage-test', name: 'StorageTest', version: '1.0.0', entry: 'App.tsx',
      }),
      'App.tsx': `
        import React from 'react';
        import { get, set } from '@hiphone/storage';

        // Run an effect synchronously at module init to write a key.
        // (In real apps this would go through useEffect; we keep it
        // simple for deterministic test behavior.)
        set('greeting', 'hi').then(() => {});

        export default function App() {
          return React.createElement('div', null, 'see storage');
        }
      `,
    });

    await install(zip);

    // Wait microtask for the set() promise inside module init
    await new Promise((r) => setTimeout(r, 50));

    // Verify via appStorage directly
    const { appStorageGet } = await import('../appStorage');
    const record = await appStorageGet('storage-test', 'storage-test:greeting');
    expect((record as { value?: unknown })?.value).toBe('hi');
  });
});
```

- [ ] **Step 8: Run tests — pass**

Run: `pnpm test src/platform/userApp/__tests__/installer.test.ts`
Expected: all passed

- [ ] **Step 9: Typecheck + Build**

Run:
```bash
pnpm typecheck
pnpm build
```
Expected: both succeed

- [ ] **Step 10: Manual smoke — a user app that persists**

Run `pnpm dev`. DevTools:

```js
const zip = await __hiphoneMakeTestZip({
  manifest: { id: 'storage-smoke', name: '存储测试', version: '1.0.0', entry: 'App.tsx' },
  files: {
    'App.tsx': `
      import React, { useState, useEffect } from 'react';
      import { get, set } from '@hiphone/storage';
      export default function App() {
        const [n, setN] = useState(0);
        useEffect(() => { get('n').then(v => v != null && setN(v)); }, []);
        const inc = () => { const next = n + 1; setN(next); set('n', next); };
        return React.createElement('div', { style: { padding: 20 } },
          React.createElement('p', null, 'Count: ' + n),
          React.createElement('button', { onClick: inc, style: { padding: 10 } }, '+1')
        );
      }
    `,
  },
});
await __hiphoneInstall(zip);
```

Expected: 桌面出图标 → 点开 → 点 +1 若干次 → **刷新页面** → 再打开 → 计数保留。

- [ ] **Step 11: Commit**

```bash
git add src/platform/userApp/sdk/storage.ts src/platform/userApp/sdk/__tests__/storage.test.ts src/platform/userApp/sdk/index.ts src/platform/userApp/__tests__/installer.test.ts
git commit -m "$(cat <<'EOF'
feat(userApp): @hiphone/storage SDK (flat key)

M2 S3 — get/set/remove/list + globalGet/globalSet backed by IDB
app-kv via appStorage primitives. appId 从 getCurrentAppId() 拿，
SDK 层透明加前缀：key → "{appId}:{key}"（per-app）或
"{appId}:__global__:{key}"（cross-context）。list() 只返回 per-app
key，global 的单独走 globalGet。S4 会把 per-app 再按 owner 分叉。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Acceptance Criteria

- [ ] `pnpm test` all green
- [ ] `pnpm build` succeeds
- [ ] `pnpm typecheck` no errors
- [ ] Manual smoke: "+1 → 刷新 → 仍在" 的小 demo 通过
- [ ] 3 commits on branch:
  - `feat(userApp): appStorage — IDB primitives for app-kv store`
  - `feat(userApp): thread appId through runtime via sync sdk context`
  - `feat(userApp): @hiphone/storage SDK (flat key)`

## Notes for Next Stage (S4)

S3 的 key 是扁平的（`{appId}:{userKey}` + `__global__` 特殊前缀）。S4 引入 owner 维度：per-app 的 `{appId}:{userKey}` 改写成 `{appId}:owner:{ownerId}:{userKey}`；`__global__` 重命名为 `global`。同时接入 `@hiphone/perspective` SDK 和 `perspectiveAware: false` 的占位行为，启动时跑一次 S3→S4 的 lazy 迁移。
