# M2 · S6: End-to-End Verification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 S1-S5 所有能力串起来，用一个完整的示例 todo-app zip 跑通父 spec §里程碑验收的 10 条标准。

**Architecture:** S6 只造"测试素材 + 集成测试"，不写新产品代码。`fixtures/todo-app/` 放一个真实多文件 todo app（App.tsx + components/TodoItem.tsx + utils.ts + icon.png + manifest.json）。vitest 集成测试装/卸载该 app，触发每个生命周期 + perspective 切换，断言整条管道的行为。手工回归清单补充 E2E 未覆盖的 UX 层面（例如 Springboard 图标真的出现/消失）。

**Tech Stack:** TypeScript, Vitest, @testing-library/react, fake-indexeddb, JSZip, pnpm.

**Parent spec:** [2026-04-17-m2-user-app-install-design.md](../superpowers/specs/2026-04-17-m2-user-app-install-design.md) §S6 + 里程碑验收 10 条

**Test command:** `pnpm test`

---

## File Structure

- **Create** `src/platform/userApp/__tests__/fixtures/todo-app/manifest.json`
- **Create** `src/platform/userApp/__tests__/fixtures/todo-app/App.tsx`
- **Create** `src/platform/userApp/__tests__/fixtures/todo-app/components/TodoItem.tsx`
- **Create** `src/platform/userApp/__tests__/fixtures/todo-app/utils.ts`
- **Create** `src/platform/userApp/__tests__/fixtures/todo-app/icon.png` (32×32 测试图)
- **Create** `src/platform/userApp/__tests__/fixtures/loadFixture.ts` — 读文本文件 + 打包成 Blob 的测试 helper
- **Create** `src/platform/userApp/__tests__/m2.e2e.test.ts` — 完整 E2E 集成测试
- **Create** `docs/plan/mockups/` 下的 M2 部署验收清单（如果要部署；否则跳过）

---

## Task 1: todo-app fixture files

**Files:**
- Create: 5 fixture files under `src/platform/userApp/__tests__/fixtures/todo-app/`

- [ ] **Step 1: Create manifest.json**

Create `src/platform/userApp/__tests__/fixtures/todo-app/manifest.json`:

```json
{
  "id": "test-todo",
  "name": "测试待办",
  "version": "1.0.0",
  "entry": "App.tsx",
  "icon": "icon.png",
  "perspectiveAware": true
}
```

- [ ] **Step 2: Create App.tsx**

Create `src/platform/userApp/__tests__/fixtures/todo-app/App.tsx`:

```tsx
import React, { useState, useEffect } from 'react';
import { get, set } from '@hiphone/storage';
import { useCurrentOwner } from '@hiphone/perspective';
import { useOnLaunch, useAppMemory } from '@hiphone/hooks';
import { TodoItem } from './components/TodoItem';
import { formatTimestamp } from './utils';

interface Todo {
  id: number;
  text: string;
  createdAt: number;
}

export default function TodoApp() {
  const { ownerName, isViewingOther } = useCurrentOwner();
  const [todos, setTodos] = useState<Todo[]>([]);
  const [draft, setDraft] = useAppMemory<string>('draft', '');

  useOnLaunch(() => {
    console.log('[todo] launched');
  });

  useEffect(() => {
    get('todos').then((raw) => {
      if (Array.isArray(raw)) setTodos(raw as Todo[]);
    });
  }, [ownerName]);

  const addTodo = () => {
    if (!draft.trim()) return;
    const next: Todo[] = [
      ...todos,
      { id: Date.now(), text: draft, createdAt: Date.now() },
    ];
    setTodos(next);
    set('todos', next);
    setDraft('');
  };

  return (
    <div style={{ padding: 20 }} data-testid="todo-app-root">
      <h1 data-testid="todo-title">
        {isViewingOther ? `${ownerName} 的待办` : '我的待办'}
      </h1>
      <div style={{ display: 'flex', gap: 8 }}>
        <input
          data-testid="todo-input"
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          style={{ flex: 1, padding: 8 }}
        />
        <button data-testid="todo-add" onClick={addTodo} style={{ padding: 8 }}>
          添加
        </button>
      </div>
      <ul data-testid="todo-list">
        {todos.map((t) => (
          <TodoItem key={t.id} text={t.text} when={formatTimestamp(t.createdAt)} />
        ))}
      </ul>
    </div>
  );
}
```

- [ ] **Step 3: Create components/TodoItem.tsx**

Create `src/platform/userApp/__tests__/fixtures/todo-app/components/TodoItem.tsx`:

```tsx
import React from 'react';

interface Props {
  text: string;
  when: string;
}

export function TodoItem({ text, when }: Props) {
  return (
    <li data-testid="todo-item" style={{ padding: 8, borderBottom: '1px solid #eee' }}>
      <span>{text}</span>
      <span style={{ marginLeft: 8, color: '#999' }}>{when}</span>
    </li>
  );
}
```

- [ ] **Step 4: Create utils.ts**

Create `src/platform/userApp/__tests__/fixtures/todo-app/utils.ts`:

```typescript
export function formatTimestamp(ts: number): string {
  const d = new Date(ts);
  const h = String(d.getHours()).padStart(2, '0');
  const m = String(d.getMinutes()).padStart(2, '0');
  return `${h}:${m}`;
}
```

- [ ] **Step 5: Create a 32×32 PNG placeholder icon**

Create `src/platform/userApp/__tests__/fixtures/todo-app/icon.png` — a 32×32 PNG. Easiest method: run a quick Node script to write one:

Create a temporary script `scripts/make-test-icon.mjs`:

```javascript
// Run once: node scripts/make-test-icon.mjs
// Generates a trivial 32x32 PNG for test fixtures.
import { writeFileSync } from 'node:fs';

// Minimal 32×32 PNG (deep-blue square, hand-crafted)
const PNG = Buffer.from(
  '89504e470d0a1a0a0000000d4948445200000020000000200806000000737a7af4' +
  '00000027494441547801edc1010d000000c2a0f74fed6f08a0000000000000000' +
  '0000006ec00100000000000001c0000001c7440a2f0000000049454e44ae426082',
  'hex',
);

writeFileSync('src/platform/userApp/__tests__/fixtures/todo-app/icon.png', PNG);
console.log('wrote icon.png');
```

Run: `node scripts/make-test-icon.mjs`
Then delete the script: `rm scripts/make-test-icon.mjs` (one-shot; not part of repo).

- [ ] **Step 6: Verify all fixture files exist**

Run:
```bash
ls -la src/platform/userApp/__tests__/fixtures/todo-app/
```
Expected: `manifest.json` / `App.tsx` / `components/TodoItem.tsx` / `utils.ts` / `icon.png` all present.

- [ ] **Step 7: Commit fixtures**

```bash
git add src/platform/userApp/__tests__/fixtures/
git commit -m "$(cat <<'EOF'
test(userApp): add multi-file todo-app fixture for M2 E2E

M2 S6 — 真实多文件 user app fixture：manifest + App.tsx（使用
@hiphone/storage/perspective/hooks）+ components/TodoItem.tsx +
utils.ts + 32x32 placeholder icon.png。下一步 m2.e2e.test.ts 打包
成 Blob 喂 installer 跑完整链路。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: loadFixture helper

**Files:**
- Create: `src/platform/userApp/__tests__/fixtures/loadFixture.ts`

- [ ] **Step 1: Write helper**

Create `src/platform/userApp/__tests__/fixtures/loadFixture.ts`:

```typescript
import { readFileSync } from 'node:fs';
import { resolve, dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import JSZip from 'jszip';

const __dirname = dirname(fileURLToPath(import.meta.url));

/**
 * Build an in-memory Blob of a user app zip from a fixture directory.
 * Node-only (uses fs) — runs in vitest with jsdom.
 */
export async function loadFixtureZip(fixtureName: string): Promise<Blob> {
  const root = resolve(__dirname, fixtureName);

  const files: Array<{ path: string; content: Buffer }> = [];
  const files_recursive = (dir: string, prefix: string): void => {
    // eslint-disable-next-line @typescript-eslint/no-var-requires
    const fs = require('node:fs') as typeof import('node:fs');
    for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
      const full = join(dir, entry.name);
      const rel = prefix ? `${prefix}/${entry.name}` : entry.name;
      if (entry.isDirectory()) files_recursive(full, rel);
      else files.push({ path: rel, content: readFileSync(full) });
    }
  };
  files_recursive(root, '');

  const zip = new JSZip();
  for (const f of files) {
    zip.file(f.path, f.content);
  }
  return zip.generateAsync({ type: 'blob' });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/platform/userApp/__tests__/fixtures/loadFixture.ts
git commit -m "$(cat <<'EOF'
test(userApp): fixture loader builds zip Blob from fixture dir

M2 S6 — loadFixtureZip(name) 递归读取 fixture 目录下所有文件，
用 JSZip 打包成内存 Blob，供 E2E 测试直接喂 installer。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: E2E integration test

**Files:**
- Create: `src/platform/userApp/__tests__/m2.e2e.test.ts`

- [ ] **Step 1: Write the E2E test**

Create `src/platform/userApp/__tests__/m2.e2e.test.ts`:

```typescript
import { beforeEach, describe, expect, it } from 'vitest';
import 'fake-indexeddb/auto';
import { render, screen, act } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { install, uninstall, loadInstalledApps } from '../installer';
import { AppScene } from '@/apps/AppScene';
import { appRegistry } from '@/platform/appRegistry';
import { useInstalledUserAppsStore } from '@/platform/stores/installedUserAppsStore';
import { useAppRuntimeStore } from '@/platform/stores/appRuntimeStore';
import { usePhoneOwnerStore } from '@/platform/stores/phoneOwnerStore';
import { registerBuiltins } from '@/apps/registerBuiltins';
import {
  getDB,
  APP_META_STORE,
  APP_SRC_STORE,
  APP_KV_STORE,
} from '@/platform/storage/idbStorage';
import { appStorageGet, appStorageListByAppId } from '../appStorage';
import { loadFixtureZip } from './fixtures/loadFixture';

async function resetAll(): Promise<void> {
  useInstalledUserAppsStore.setState({ apps: [] });
  useAppRuntimeStore.setState({ appEvents: {}, openParams: {} });
  usePhoneOwnerStore.setState({ phoneOwnerId: null });
  appRegistry.list().forEach((e) => appRegistry.unregister(e.id));

  const db = await getDB();
  const stores = [APP_META_STORE, APP_SRC_STORE, APP_KV_STORE];
  await new Promise<void>((resolve) => {
    const tx = db.transaction(stores, 'readwrite');
    stores.forEach((s) => tx.objectStore(s).clear());
    tx.oncomplete = () => resolve();
  });

  // Re-register builtins so AppScene has them (not strictly needed for
  // user app tests but matches production load order)
  registerBuiltins();
}

describe('M2 E2E — todo-app full lifecycle', () => {
  beforeEach(() => resetAll());

  it('#3: installs the multi-file fixture zip and populates all backends', async () => {
    const zip = await loadFixtureZip('todo-app');
    const result = await install(zip);

    expect(result.id).toBe('test-todo');
    expect(result.isUpgrade).toBe(false);

    // installedUserAppsStore
    const apps = useInstalledUserAppsStore.getState().apps;
    expect(apps.map((a) => a.id)).toContain('test-todo');
    const todoEntry = apps.find((a) => a.id === 'test-todo')!;
    expect(todoEntry.perspectiveAware).toBe(true);
    expect(todoEntry.iconDataUrl).toMatch(/^data:image\/png;base64,/);

    // appRegistry
    const entry = appRegistry.get('test-todo');
    expect(entry?.type).toBe('user');
    expect(entry?.perspectiveAware).toBe(true);

    // IDB app-meta populated
    const db = await getDB();
    const meta = await new Promise<unknown>((resolve) => {
      const tx = db.transaction(APP_META_STORE, 'readonly');
      const req = tx.objectStore(APP_META_STORE).get('test-todo');
      req.onsuccess = () => resolve(req.result);
    });
    expect(meta).toBeDefined();
  });

  it('#3 + #5: renders the user app through AppScene with multi-file imports', async () => {
    await install(await loadFixtureZip('todo-app'));

    render(<AppScene appId="test-todo" />);

    expect(await screen.findByTestId('todo-app-root')).toBeInTheDocument();
    expect(screen.getByTestId('todo-title').textContent).toMatch(/我的待办/);
  });

  it('#3 + #4: user can write storage via SDK and it persists', async () => {
    await install(await loadFixtureZip('todo-app'));

    const user = userEvent.setup();
    render(<AppScene appId="test-todo" />);

    await user.type(screen.getByTestId('todo-input'), '买牛奶');
    await user.click(screen.getByTestId('todo-add'));

    // Assert storage write
    const record = await appStorageGet(
      'test-todo',
      'test-todo:owner:me:todos',
    );
    expect(record).toBeDefined();
    expect((record as { value: any[] }).value[0].text).toBe('买牛奶');
  });

  it('#4: loadInstalledApps rebuilds state after "reload"', async () => {
    await install(await loadFixtureZip('todo-app'));

    // Simulate reload: clear memory state, keep IDB
    useInstalledUserAppsStore.setState({ apps: [] });
    appRegistry.list().forEach((e) => appRegistry.unregister(e.id));

    await loadInstalledApps();

    expect(appRegistry.get('test-todo')).toBeDefined();
    expect(
      useInstalledUserAppsStore.getState().apps.some((a) => a.id === 'test-todo'),
    ).toBe(true);
  });

  it('#7: perspective-aware user app isolates storage per owner', async () => {
    await install(await loadFixtureZip('todo-app'));

    const user = userEvent.setup();
    const { unmount } = render(<AppScene appId="test-todo" />);
    await user.type(screen.getByTestId('todo-input'), 'player-task');
    await user.click(screen.getByTestId('todo-add'));
    unmount();

    // Switch to char-001 view
    act(() => usePhoneOwnerStore.setState({ phoneOwnerId: 'char-001' }));

    render(<AppScene appId="test-todo" />);
    const todos = screen.queryAllByTestId('todo-item');
    expect(todos.length).toBe(0);

    await user.type(screen.getByTestId('todo-input'), 'char-task');
    await user.click(screen.getByTestId('todo-add'));

    // Verify isolation in IDB
    const playerRec = await appStorageGet(
      'test-todo', 'test-todo:owner:me:todos',
    );
    const charRec = await appStorageGet(
      'test-todo', 'test-todo:owner:char-001:todos',
    );
    expect((playerRec as any).value[0].text).toBe('player-task');
    expect((charRec as any).value[0].text).toBe('char-task');
  });

  it('#8: perspectiveAware=false app shows placeholder in char view', async () => {
    // Install a second fixture-less app with perspectiveAware=false
    const zip = await loadFixtureZip('todo-app');
    // Patch manifest to perspectiveAware=false in-memory:
    const JSZipModule = (await import('jszip')).default;
    const zipObj = await JSZipModule.loadAsync(zip);
    const manifestStr = await zipObj.file('manifest.json')!.async('string');
    const parsed = JSON.parse(manifestStr);
    parsed.id = 'no-persp';
    parsed.perspectiveAware = false;
    zipObj.file('manifest.json', JSON.stringify(parsed));
    const rezipped = await zipObj.generateAsync({ type: 'blob' });

    await install(rezipped);

    act(() => usePhoneOwnerStore.setState({ phoneOwnerId: 'char-001' }));

    render(<AppScene appId="no-persp" />);
    // Should not render the todo content
    expect(screen.queryByTestId('todo-app-root')).toBeNull();
  });

  it('#9: useOnLaunch fires on open; useAppMemory resets on kill', async () => {
    await install(await loadFixtureZip('todo-app'));

    const user = userEvent.setup();
    const { unmount } = render(<AppScene appId="test-todo" />);
    await user.type(screen.getByTestId('todo-input'), 'draft text');
    unmount();

    // Simulate kill: bump kill nonce
    act(() => {
      useAppRuntimeStore.setState((s) => ({
        appEvents: {
          ...s.appEvents,
          'test-todo': {
            launch: 0, resume: 0, background: 0,
            kill: (s.appEvents['test-todo']?.kill ?? 0) + 1,
          },
        },
      }));
    });

    // Re-render
    render(<AppScene appId="test-todo" />);
    // draft should reset to initial '' because useAppMemory clears on kill
    expect((screen.getByTestId('todo-input') as HTMLInputElement).value).toBe('');
  });

  it('#10: uninstall clears all three IDB stores + Registry + Springboard', async () => {
    await install(await loadFixtureZip('todo-app'));

    // Write per-owner and global data
    const user = userEvent.setup();
    const { unmount } = render(<AppScene appId="test-todo" />);
    await user.type(screen.getByTestId('todo-input'), 'before-uninstall');
    await user.click(screen.getByTestId('todo-add'));
    unmount();

    const keysBefore = await appStorageListByAppId('test-todo');
    expect(keysBefore.length).toBeGreaterThan(0);

    await uninstall('test-todo');

    // All three IDB stores empty for this app
    const db = await getDB();
    const meta = await new Promise<unknown>((resolve) => {
      const tx = db.transaction(APP_META_STORE, 'readonly');
      const req = tx.objectStore(APP_META_STORE).get('test-todo');
      req.onsuccess = () => resolve(req.result);
    });
    const src = await new Promise<unknown>((resolve) => {
      const tx = db.transaction(APP_SRC_STORE, 'readonly');
      const req = tx.objectStore(APP_SRC_STORE).get('test-todo');
      req.onsuccess = () => resolve(req.result);
    });
    const keysAfter = await appStorageListByAppId('test-todo');
    expect(meta).toBeUndefined();
    expect(src).toBeUndefined();
    expect(keysAfter).toEqual([]);

    // Registry cleared
    expect(appRegistry.get('test-todo')).toBeUndefined();
    // installedUserAppsStore cleared
    expect(
      useInstalledUserAppsStore.getState().apps.some((a) => a.id === 'test-todo'),
    ).toBe(false);
  });
});
```

- [ ] **Step 2: Run E2E test**

Run: `pnpm test src/platform/userApp/__tests__/m2.e2e.test.ts`
Expected: 8 passed (covers acceptance items #3-#10)

If any test fails, fix the underlying issue. Do NOT disable the test. Common issues and fixes:
- Fixture files not discoverable → check path resolution in loadFixture.ts
- `icon.png` missing → re-run the one-shot icon generator script
- `test-todo` perspective placeholder doesn't appear → check AppScene handles `entry.perspectiveAware=false + isViewingOther` path correctly (M1 code)

- [ ] **Step 3: Commit E2E test**

```bash
git add src/platform/userApp/__tests__/m2.e2e.test.ts
git commit -m "$(cat <<'EOF'
test(userApp): M2 E2E integration — full todo-app lifecycle

M2 S6 — 8 个用例覆盖父 spec 里程碑验收 #3-#10：
  - 装多文件 zip → IDB / Registry / Store 全部更新
  - AppScene 渲染 user app
  - 点击写 storage → IDB 反映
  - 模拟刷新 → loadInstalledApps 恢复
  - 切 perspective → 数据隔离
  - perspectiveAware=false → 显示占位
  - 触发 kill nonce → useAppMemory reset
  - uninstall → 三个 IDB store + Registry + installedUserAppsStore
    全部清空

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Acceptance run + verification checklist

- [ ] **Step 1: Run full suite — verify all green**

Run: `pnpm test`
Expected: all tests pass (M1 + M2 S1-S6 full)

- [ ] **Step 2: Build**

Run: `pnpm build`
Expected: success with no warnings.

- [ ] **Step 3: Verify acceptance #2 — DEV API stripped, Sucrase included**

Run:
```bash
grep -r "__hiphoneInstall" dist/ || echo "OK (stripped)"
grep -r "sucrase" dist/ | head -3
grep -r "jszip" dist/ | head -3
```
Expected:
- `__hiphoneInstall` → `OK (stripped)`
- `sucrase` / `jszip` → has matches

- [ ] **Step 4: Typecheck**

Run: `pnpm typecheck`
Expected: no errors

- [ ] **Step 5: Manual smoke round (acceptance #3-#10 via real browser)**

Run `pnpm dev`. Open browser + DevTools:

```js
const zip = await __hiphoneMakeTestZip('todo');
await __hiphoneInstall(zip);
```

Then in the app:
- Verify 桌面出 "测试待办" 图标（acceptance #3）
- Click icon → see todo UI
- Add a todo "a" → refresh page → reopen → "a" still there（acceptance #4）
- Multi-file demo was covered by S2 smoke; skip
- Switch phoneOwner via existing UI to some character → todos empty → add "char-a" → switch back → player's "a" visible（acceptance #7）
- Install a second zip with perspectiveAware=false → in char view, placeholder shown（acceptance #8）
- Add a draft → home → reopen → draft preserved → kill (swipe up) → reopen → draft empty（acceptance #9）
- `await __hiphoneUninstall('test-todo')` → icon disappears（acceptance #10）

Document any red flags in a scratch note but do not fix here (open a follow-up task).

- [ ] **Step 6: Summary commit (acceptance log)**

```bash
# No new files — just record the passing run via tag-style commit
git commit --allow-empty -m "$(cat <<'EOF'
chore(m2): M2 milestone acceptance — 10 criteria met

Automated: S1-S6 unit + integration + m2.e2e all green.
Build: pnpm build succeeds; sucrase + jszip in dist/; __hiphoneInstall stripped.
Manual: todo-app.zip install → run → persist → perspective → kill reset → uninstall
all verified in DEV.

M2 done — ready for merge or continue to M3.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Acceptance Criteria

M2 里程碑 10 条（对齐父 spec §里程碑验收）:

- [ ] #1: M1 所有现有测试 + 新 M2 所有测试全绿（`pnpm test`）
- [ ] #2: `pnpm build` 无警告；`grep sucrase dist/` 有结果；`grep __hiphoneInstall dist/` 无结果
- [ ] #3: DevTools `__hiphoneInstall(await __hiphoneMakeTestZip('todo'))` → 桌面出图标 → 点开运行
- [ ] #4: 安装后刷新 → 图标 + 数据仍在
- [ ] #5: 多文件 zip 能装能跑（S2 覆盖）
- [ ] #6: 循环依赖 zip 能装能跑（S2 覆盖）
- [ ] #7: `perspectiveAware: true` 的 app 按 owner 自动隔离
- [ ] #8: `perspectiveAware: false` 的 app 在角色视角显示占位
- [ ] #9: useOnLaunch/Resume/Background/Kill + useAppMemory 正确触发
- [ ] #10: uninstall 清三个 IDB store + Registry + Springboard store

## Notes for Next Stage (M3)

M2 落地后下一步是 M3：
- App Store UI（本地上传页 + 已安装管理页）—— 把 `__hiphoneInstall` 从 DEV API 换成真 UI
- 剩余 SDK：`@hiphone/nav`（Deep Link + useOpenParams 真正有数据）、`@hiphone/fetch`、`@hiphone/ai`、`@hiphone/toast`
- Tool Registry + 内置工具迁移 + AI App Builder V1
- Twind（从 M2 推后到这里的运行时 Tailwind 子系统）
