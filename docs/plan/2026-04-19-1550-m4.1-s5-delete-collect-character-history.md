# M4.1 S5 — Delete `collectCharacterHistory`

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove `collectCharacterHistory` from `xingYuDataStore.ts` and all its callers + imports. After S4 completed, nothing *calls* it; this stage formally deletes it and the now-dead supporting code. Small, mechanical clean-up stage — but required to keep the codebase honest and prevent later drift.

**Architecture:** Pure deletion. `collectCharacterHistory` was the last bridge between xingYuDataStore.messages and AI prompt building. Removing it cements memoryStore as the sole source of truth for AI context.

**Tech Stack:** TypeScript, Vitest.

**Parent spec:** [`docs/superpowers/specs/2026-04-19-m4.1-ai-sdk-xingyu-migration-design.md`](../superpowers/specs/2026-04-19-m4.1-ai-sdk-xingyu-migration-design.md) §5.5

**Test command:** `pnpm test`

---

## File Structure

- **Modify** `src/apps/XingYu/xingYuDataStore.ts` — delete `collectCharacterHistory` export + its imports (`HistoryMessage` if only used by it)
- **Modify** `src/platform/ai/promptAssembly.ts` — remove now-unused `HistoryMessage` export if no consumers remain
- **Modify** `src/platform/ai/heartbeatAgent.ts` — remove the `collectCharacterHistory` / `triggerCompression` imports from xingYuDataStore (no longer needed after S4; triggerCompression is handled in S6)
- **Modify** `src/platform/ai/aiChatEngine.ts` — remove the `collectCharacterHistory` import
- **Modify** any test files that imported these symbols for their setup — rewrite to populate memoryStore directly via `useCharacterMemory.getState().append(...)`

---

## Task 1: Prove no live callers + delete the function

**Files:**
- Modify: `src/apps/XingYu/xingYuDataStore.ts`
- Modify: `src/platform/ai/heartbeatAgent.ts`
- Modify: `src/platform/ai/aiChatEngine.ts`
- Modify: `src/platform/ai/promptAssembly.ts`

- [ ] **Step 1: Prove nothing still calls `collectCharacterHistory`**

Run: `grep -rn "collectCharacterHistory" src/ --include="*.ts" --include="*.tsx"`

Expected output: only the export site in `xingYuDataStore.ts` + possibly test files that still import it. If any non-test `src/` file imports it, STOP — S4 didn't complete properly. Fix by migrating that caller before proceeding.

- [ ] **Step 2: Delete the function and its imports**

In `src/apps/XingYu/xingYuDataStore.ts`:

- Delete the entire `collectCharacterHistory` function (lines 53-107 or thereabouts).
- Delete the `import { assemblePrompt, type HistoryMessage } from '@/platform/ai/promptAssembly';` import line's `HistoryMessage` type import (keep `assemblePrompt`).

In `src/platform/ai/heartbeatAgent.ts`:

- Remove `collectCharacterHistory` from the `import { useXYData, collectCharacterHistory, triggerCompression } from '@/apps/XingYu/xingYuDataStore';` line. Leave `triggerCompression` alone for now — S6 will handle it. Alternatively, if you notice triggerCompression is also dead (S4 deleted its call site), remove it too and update S6's notes.

In `src/platform/ai/aiChatEngine.ts`:

- Remove `collectCharacterHistory` from the same import.

In `src/platform/ai/promptAssembly.ts`:

- Find `HistoryMessage` — if there are no remaining consumers (grep for `HistoryMessage` across src/), delete the type and any helper functions that only operated on it.
- Run `grep -rn "HistoryMessage" src/` to double-check before deleting.

- [ ] **Step 3: Update any test files that imported `collectCharacterHistory`**

Run: `grep -rn "collectCharacterHistory" src/ --include="*.test.ts"`

For each match: change the test to populate memoryStore directly via:

```typescript
import { useCharacterMemory } from '@/platform/ai/characterMemoryStore';

// In setup:
useCharacterMemory.getState().append('char-001', {
  role: 'user', speakerId: 'me', content: '测试', source: 'xingyu',
});
```

Do NOT add any `collectCharacterHistory` stubs — just migrate to the new shape.

- [ ] **Step 4: Run the full suite**

Run: `pnpm test && pnpm typecheck && pnpm build`
Expected: all green. If anything breaks, it's a genuine missed caller — fix by migrating to memoryStore, never by re-adding `collectCharacterHistory`.

- [ ] **Step 5: Final grep sanity**

Run: `grep -rn "collectCharacterHistory\|HistoryMessage" src/ --include="*.ts" --include="*.tsx"`

Expected: empty (or only comments / docs with the name).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "refactor(ai): delete collectCharacterHistory + HistoryMessage

Final removal of the xingYuDataStore → AI prompt bridge. memoryStore
is now the sole source of AI context. All tests still green.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Acceptance for S5

- [ ] `pnpm test` all green
- [ ] `pnpm typecheck` clean
- [ ] `pnpm build` succeeds
- [ ] Grep: `collectCharacterHistory` returns empty from `src/`
- [ ] Grep: `HistoryMessage` returns empty from `src/` (or only doc comments)
- [ ] Manual smoke: XingYu chat + heartbeat tick both still work end-to-end (prompts assembled from memoryStore)
