# App 图标裁剪圆角遮罩修复计划

## 用户反馈

- 用户截图指出圆角裁剪框外侧仍然露出一个正方形区域。
- 这个正方形来自遮罩实现：四块矩形遮罩只挖出了正方形透明区域，圆角外的四个角没有被遮住。

## 关键决策

- 将遮罩改成 SVG mask，一次性挖出圆角矩形透明区域。
- 保留中心圆角裁剪框线、框外压暗图片、拖动缩放和保存逻辑。
- 让 SVG 遮罩圆角和可见裁剪框圆角使用同一组比例，避免再次出现方角残留。

## 验证

- 运行 Settings 图标编辑相关单测。
- 运行 TypeScript 类型检查。
- 用真实浏览器确认圆角外不再露出方形透明块。
