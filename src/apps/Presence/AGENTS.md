# src/apps/Presence/ — 在场 App 规范

## 关键规则

1. 用户可见文案使用“角色”，不要写“AI 角色”。
2. 角色选择只展示头像和名字；不要展示关系、上次活动、最近互动等当前数据不存在的字段。
3. 场景内互动是自由小说式文本，不要求 JSON、固定字段、固定动作协议。
4. 场景进行中不得写入 `characterMemoryStore`；只在用户离开并确认保存后写入一条总结记忆。
5. 已完成的「在场记录」只能查看，不能继续互动。延续体验必须创建新 session。
6. 场景页不使用聊天气泡，不展示“场景笔记”。
